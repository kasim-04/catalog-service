# 🎬 Catalog Service — Онлайн-кинотеатр

**Catalog Service** — микросервис каталога фильмов для распределённой системы онлайн-кинотеатра.  
Сервис отвечает за хранение и предоставление информации о фильмах, жанрах, странах и персонах, а также за административное управление каталогом фильмов.

---

## 📌 Общее описание

Микросервис предоставляет REST API для:
- просмотра каталога фильмов;
- поиска, фильтрации, сортировки и пагинации;
- получения справочной информации (жанры, страны, персоны);
- административного управления каталогом фильмов.

Сервис является независимым backend-компонентом и может использоваться
как частью распределённой системы онлайн-кинотеатра, так и отдельно.

---

## 🛠 Используемые технологии

**Backend**
- Python 3.11
- FastAPI
- Uvicorn
- SQLAlchemy
- Pydantic

**Database**
- PostgreSQL

**Infrastructure**
- Docker
- Docker Compose

---

## 🚀 Запуск проекта

### 1. Клонирование репозитория
```bash
git clone <repo_url>
cd cinema-catalog
```

### 2. Переменные окружения

Создайте файл `.env` в корне проекта:

```env
DATABASE_URL=postgresql+psycopg2://postgres:postgres@db:5432/catalog
ADMIN_TOKEN=supersecret123
```

### 3. Запуск через Docker Compose
```bash
docker compose up --build
```

---

## 🌐 Доступные хосты

| Назначение | URL |
|-----------|-----|
| API | http://localhost:3000 |
| Swagger UI | http://localhost:8000/docs |

---

## 📖 API

### Публичные эндпоинты
- `GET /api/movies`
- `GET /api/movies/{id}`
- `GET /api/genres`
- `GET /api/countries`
- `GET /api/persons`

### Административные эндпоинты
Требуют заголовок:
```
X-Admin-Token: <ADMIN_TOKEN>
```

- `POST /api/admin/movies`
- `PUT /api/admin/movies/{id}`
- `DELETE /api/admin/movies/{id}`
